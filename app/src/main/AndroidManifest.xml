<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:screenOrientation="portrait"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ultima">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
    </activity>
        <activity
            android:name=".LogicBufferPage"/>
        <activity
            android:name=".CheckBoxCode"/>
        <activity
            android:name=".LogicTxtPage"/>
        <activity
            android:name=".LogicGuessingGame"/>
        <activity
            android:name=".RiddlesCode"/>
        <activity
            android:name=".DialogboxCode"/>
        <activity
            android:name=".Mathematics"/>
        <activity
            android:name=".WorkingWithData"/>
        <activity
            android:name=".LogicPlayeer"/>

</application>

</manifest>